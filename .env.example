# ==============================================================================
# TEMİN 360 - Environment Variables Configuration (.env.example)
# ==============================================================================

# 1. YEREL GELİŞTİRME (LOCAL MODE)
# Docker ile sadece veritabanını başlatıp (pnpm db:up), servisleri yerelde çalıştırmak için:
DATABASE_URL="postgresql://postgres:temin360pass@localhost:5432/temin360_db?schema=public"
NEXT_PUBLIC_API_URL="http://localhost:4000/api/v1"
BACKEND_PORT=4000
WEB_PORT=3000

# 2. KONTEYNER / SUNUCU ORTAMI (SERVER / DOCKER COMPOSE MODE)
# Docker Compose ile tüm servisler tek ağda çalışırken:
# DATABASE_URL="postgresql://postgres:temin360pass@db:5432/temin360_db?schema=public"
# NEXT_PUBLIC_API_URL="http://backend:4000/api/v1"

# Güvenlik & İstemci Anahtarları
API_SECRET_KEY="dta_live_8e4a90f1b2c3d4e59071f"
CORS_ORIGIN="http://localhost:3000,http://127.0.0.1:3000"
